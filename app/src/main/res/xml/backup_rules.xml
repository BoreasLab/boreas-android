<?xml version="1.0" encoding="utf-8"?>
<!--
  The same answer as data_extraction_rules.xml, in the older schema, for the two
  API levels that only understand this one.

  `android:dataExtractionRules` is read from Android 12; this app's floor is 29,
  so on 29 and 30 that file is ignored entirely and this one is consulted instead.

  Both are redundant while `android:allowBackup` is false, which disables backup
  on every version this app runs on. They are here so that stays a choice rather
  than the only thing standing between the stored set and a backup transport:
  turning `allowBackup` back on should not silently export anything.
-->
<full-backup-content>
    <exclude domain="root" path="." />
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="external" path="." />
    <exclude domain="device_root" path="." />
    <exclude domain="device_file" path="." />
    <exclude domain="device_database" path="." />
    <exclude domain="device_sharedpref" path="." />
</full-backup-content>
